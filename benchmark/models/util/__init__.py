from .util import mask_, d, here, contains_nan, adj, adj_triples, adj_triples_tensor, sum_sparse, contains_inf, contains_nan, \
      logsoftmax, simple_normalize, batchmm, spmm, tic, toc, schedule, Lambda, Debug, block_diag, prod, batch, \
      get_slug, eval, eval_simple, truedicts, initialize, compute_entity_frequency, read_config

